int main()
{
    int n1, n2, n3;
    printf("Input 3 Different Numbers\n");
    scanf("%d %d %d", &n1, &n2, &n3);

    if(n1>n2 && n1>n3)
        printf("%d is the Highest", n1);
    else if (n2>n1 && n2>n3)
        printf("%d is the Highest", n2);
    else
        printf("%d is the Highest", n3);

    return 0;
}
