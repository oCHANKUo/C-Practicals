#include <stdio.h>

int main() {
    int n = 10;
    int a = 0, b = 1, nextTerm;

    printf("First %d numbers of Fibonacci Sequence: %d %d ", n, a, b);

    for (int i = 2; i < n; i++) {
        nextTerm = a + b;
        printf("%d ", nextTerm);
        a = b;
        b = nextTerm;
    }

    printf("\n");

    return 0;
}
